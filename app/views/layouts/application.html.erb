<!DOCTYPE html>
<html>
  <head>
    <!-- this will alter or show what your writing not just Message board title -->
    <title><%=yield(:page_title)%> | Message Board </title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag  'application', media: 'all', 'data-turbolinks-track': 'true' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'true' %>
  </head>

  <body id="decor">

    <div class="navbar navbar-default">
      <div class="container">
          <div class="navbar-header">
            <!-- <%= link_to "Message Board", root_path, class: "navbar-brand" %> -->

            <% if current_page?(root_path) %>
                <h4 class:"navbar-brand">Message Board</h4>
              <% else %>
                  <h4> <%= link_to "Back to All Posts", root_path %></h4>
              <% end %>

          </div>
            <ul class="navbar-nav nav">
              <li id="sign_up"><%= link_to "Sign Up", new_user_registration_path %></li>
              <% if user_signed_in? %>
                <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
              <% else %>
                <li id="log_in"><%= link_to "Log In", new_user_session_path %></li>
              <% end %>
            </ul>
            <!-- if user_signed_in they have access or just link to create new post  -->
              <% if user_signed_in? %>
              <p><%= link_to "New Post", new_post_path, class: "navbar-right navbar-text navbar-link" %></p>
                <% end %>
      </div>
    </div>




    <div class="container">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>

    <div class="container">
        <%= yield %>
    </div>

  </body>
</html>
